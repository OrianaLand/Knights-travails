// Responsible for search algorithms (BFS).
// Does NOT know what a knight is.
// It receives:
// - a start node
// - a target node
// - a way to get neighbors

// Possible future responsibility:
// - Track visited positions
// - Build and return the shortest path

export function bfs(start, end, getNeighbors) {
  // getNeighbors(node) -> array of neighboring nodes
  // returns shortest path from start to end
}
